import React from "react";
import Styles from "./header.module.css";

const Header = () => {
  return (
    <div>
      <p className={Styles.text}>Hello there</p>
    </div>
  );
};

export default Header;
